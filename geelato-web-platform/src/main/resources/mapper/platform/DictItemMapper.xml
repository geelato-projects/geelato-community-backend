<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.geelato.web.platform.srv.dictionary.mapper.DictItemMapper">

    <!-- 根据字典编码查询字典项 -->
    <select id="selectByDictCode" parameterType="string" resultType="map">
        SELECT 
            di.item_code AS itemCode,
            di.item_name AS itemName
        FROM platform_dict_item di
        INNER JOIN platform_dict d ON di.dict_id = d.id
        WHERE d.dict_code = #{dictCode}
          AND di.enable_status = 1
          AND d.enable_status = 1
        ORDER BY di.seq_no ASC, di.item_code ASC
    </select>

</mapper>